export type TrackData = {
    name: string;
    artistId: string;
    likes: number;
    layerrs: number;
    waveformData: number[];
    duration: number;
}

export type ArtistData = {
    name: string;
}